# Backend lyrify with nodejs and javascript

## Database with Mysql, import this `backup.sql` into your database local

## Configuration env
Create an `.env` files and copied in this code :
```bash
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_DATABASE=
PORT=
```